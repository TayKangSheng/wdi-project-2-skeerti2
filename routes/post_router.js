var express = require('express')
router = express.Router()
var Chef = require('../models/chef')
var Post = require('../models/post')


// router.post('/chefs/:id', postController.create )

module.exports = router
